Clone the repo: git clone https://github.com/iolee/sqlCustomer.git

Start Docker: Open a terminal in that folder and run docker-compose up -d.


View the app: Open a browser and go to localhost:8080.
